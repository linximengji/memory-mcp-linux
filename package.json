{
  "name": "memory-mcp",
  "version": "1.0.0",
  "type": "module",
  "description": "Memory MCP server — file-based memory storage with LLM rerank",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
